# Assign ownership and permissions to existing files, directories, device files, named pipes, etc.
# <name>                <type>  <mode>  <uid>   <gid>   <major> <minor> <start> <inc>   <count>
/etc/monitrc            f       600     0       0   -   -   -   -   -
